# MODULE_STATUS.md

_Last updated: 2026-07-29 — Full project audit against `docs/Jewellery_Retail_Software_PRD.md`, `docs/Jewellery_ERP_Developer_Handbook (1).md`, and `.ai/`, refreshed after Milestones 21–27 and 48 (Phases 1–8 complete). Every row below was re-verified directly against current `src/` code (post Milestone 1, 2 & 3), not carried forward from the prior draft of this file. See `CURRENT_PROGRESS.md` §3 for the detailed missing-screens/dialogs/workflows/nav/components/logic/responsive breakdown behind each row._

## Legend
- **Not Started**: No UI screen, component, or logic implemented.
- **UI Mockup Only**: Visual layout exists, but lacks real underlying domain model, data persistence, or business rules.
- **Partial**: Functional UI exists and covers part of the module correctly, but has real missing sub-features (see `CURRENT_PROGRESS.md`).
- **Partial / Incorrect**: Functional UI exists but contains domain-correctness bugs (see `KNOWN_ISSUES.md` for any still open).
- **Done**: Fully production-ready, validated frontend module matching PRD & Handbook specifications.

---

## Detailed PRD Module & Handbook Phase Implementation Matrix

| PRD Module (§) | Handbook Phase | Current UI Status | Detailed Gap & Missing Frontend Features Analysis |
|---|---|---|---|
| **1. Domain Glossary & Metal Standards** | Phase 1 | **UI Mockup Only** | Fixed metal-type dropdown (24K/22K/18K/Silver/Platinum) with no underlying purity-fraction master; no Fine Gold (24K) equivalent weight display anywhere in the UI; no multi-metal composition support. |
| **2. Multi-Branch & Location System** | Phase 2 §2.10 | **Partial** | **(2026-07-28, Milestones 19–20)** Real `Branch` entity (`branchCode`, `gstin`, `stateCode`, `invoiceSeriesPrefix`, `defaultStockOwnershipType`, `rateOverrides`) with a header branch switcher; Catalog/Stones/Job Bags are branch-scoped, and per-branch metal rate overrides resolve through `resolveMetalRate()`. Per **D-5**, Party and Metal/Purity masters are deliberately tenant-wide, never branch-scoped. Invoice numbering is a per-GSTIN series (closes `KNOWN_ISSUES.md` #11b). Inter-Branch Stock Transfer is live (`src/lib/stockTransfer.ts`, Catalog → Stock Transfers): dispatch → in-transit → per-piece accept/reject at destination, with **D-7** enforced structurally because `TransferInTransit` is not a sellable state. Missing: no branch-comparison/consolidated stock report (M28-series), no branch-level user permissioning (M45), e-Way Bill is flagged but not generated (M22), thresholds are not yet genuinely per-state (M34). |
| **3. Personas & RBAC Roles** | Phase 12 | **UI Mockup Only** | Login has a cosmetic role-selection dropdown; once logged in, every route/action is accessible regardless of role. No Admin Role/Permission screen, no Supervisor PIN modal, no maker-checker, no route-level guards. |
| **4.1 Metal Master & Rate Master** | Phase 2 §2.2/§2.3 | **Partial** | **(2026-07-29, Milestone 48 — pulled forward)** Rates are now an **append-only** `MetalRateVersion[]` history with full audit trail (who set it, when, why), closing the last decision **D-4** violation. A timestamped `resolveRateAt()` means an old invoice resolves the rate it was billed at. PRD §4.2's fat-finger guard is live at 5% with a mandatory reason beyond it and a decimal-slip callout beyond 50%. 24K-base purity derivation is offered as a suggestion rather than applied. Branch-level rate override already existed via `resolveMetalRate()` (M19). Missing: no separate purity master screen; no maker-checker *approval* on a rate change (it records a reason but needs no second person — that depends on RBAC, M32/M45). |
| **4.2 Item / Design Master** | Phase 2 §2.5 | **Partial** | **(2026-07-25, Milestone 3)** `ItemDesign` (template) and `Tag` (physical, individually-weighed piece) are now genuinely separate types, resolving Handbook D-6. `CatalogManager.tsx`'s Item Design Templates tab covers create/browse/delete with a live tagged-stock count per design. Missing: no design-family cross-purity rollup (e.g. linking a 22K and 18K version of the same design), no ad-hoc "Custom Item" catalog-free billing path, no per-branch availability flag, no design edit (create/delete only). |
| **4.3 Party Master (Customer/Karigar/Supplier)** | Phase 2 §2.6 | **Partial** | `Customer` entity covers retail CRM well (tier, loyalty, lifetime spend, one scheme). Missing: PAN/GSTIN/Aadhaar/KYC fields entirely absent from the type; no unified Customer+Supplier "Party" ledger (Karigar and Customer are unrelated entities); no credit limit/opening balance; no PAN-missing-alert. |
| **4.4 Making Charge & Wastage Master** | Phase 2 §2.7 | **Partial** | Per-item `wastagePercent`/`makingChargeType`/`makingChargeValue` exist and are now correctly consumed by the billing calculation engine (fixed 2026-07-25, Milestone 2). Missing: no category-level slab master table/screen, no three-tier override hierarchy (Category Slab → Item Design default → Transaction-time override with approval) — only 2 of 3 tiers exist, and there's no shared resolution function. |
| **4.5 Stone & Diamond Rate Master** | Phase 2 §2.8 | **Partial** | `StoneManager.tsx` tracks loose-stone vault inventory (lot, cut, color, clarity, carats) with ad hoc per-lot pricing entered at intake. Missing: no 4Cs rate-card/slab master (rate by cut/color/clarity/carat-range), no certificate PDF viewer/upload, not wired into Billing's `stoneCharge` field (still a free-typed number there). |
| **4.6 Tax Master** | Phase 2 §2.9 | **Done** | **(2026-07-29, Milestone 21)** `src/lib/taxMaster.ts` + a Tax Master tab on Billing. Real HSN/SAC rows (7113/7102/7103/7108/7106/SAC 9988) with `notificationRef` traceability and **effective-date versioning**: a rate change appends a new row and closes the previous one, never overwrites, so an old invoice still resolves the rate it was billed at (PRD §9.2 forbids hardcoding rates as immutable). The `0.03` constant is gone. The 7102 diamond row is deliberately defined-but-unapplied pending the CA sign-off in `HANDOFF.md` item 1 — the engine supports the split, so authorising it is a data change. |
| **4.7 Branch / Location Master** | Phase 2 §2.10 | **Partial** | **(2026-07-28, Milestone 19)** `Branch` entity and switcher exist (see Module 2 above), seeded with three branches and validated by `validateBranch()`. Missing: no dedicated Branch Master *admin screen* to create/edit branches from the UI — branches come from seed data, and editing GSTIN/state/series is a Settings-module concern (M44). |
| **5. Inventory & Atomic Tagging** | Phase 3 | **Partial** | **(2026-07-25, Milestones 3–6)** `Tag` is a real, separate entity (see Module 4.2) with a genuine `huid?: string` and `stockOwnershipType` field/badge/filter. `Tag.status` is now the **full enforced 12-state lifecycle** (`RawMetal → IssuedToKarigar → ReceivedFromKarigar → PendingHallmark → Hallmarked → InStock → {InShowcase, OutForJobwork, MemoOut, TransferInTransit, Sold, DamagedOrMelted}`) governed by `src/lib/tagStateMachine.ts` — every UI status change is validated through `canTransition()` and illegal moves are rejected with a visible error (Handbook D-6/D-7). Real, scannable QR + CODE128 barcode generation via `ui/TagCode.tsx` (Milestone 5). Stock Audit / reconciliation tab with missing-and-extra flagging and a weight-wise discrepancy report (Milestone 6). Still missing: at-cost/at-market dual valuation; the Memo-Out *workflow* (the `MemoOut` state exists, but no dedicated issue/return screen drives it). |
| **6. Procurement & Karigar Job-Work** | Phase 4 | **Partial** | **(2026-07-27, Milestones 16-18)** Karigar balances are now DERIVED from an append-only `KarigarLedgerEntry` ledger with a full statement view, closing `KNOWN_ISSUES.md` #10; weight and money are structurally separate ledgers per D-2. Wastage is assessed in **Fine Gold Equivalent** (PRD §6.2) instead of raw grams, which had been understating losses across mixed purities. `WorkOrder` and `JobBag` are unified into one `JobWork` aggregate, so the ledger and kanban views can no longer drift apart, and stage advance is guarded so Hallmark cannot be skipped. Excess wastage is flagged for owner review with a write-off/recover decision and mandatory note, plus a scrap & unused-stone return receipt. Still missing: no Procurement/GRN screen (Milestones 38-41); no Melting workflow (Milestone 43); a completed job does not yet auto-create a real `Tag` (`JobWork.producedTagId` exists but nothing populates it); no outside-job-work GST/reverse-charge recording. |
| **7. Billing & POS Calculation Engine** | Phase 5 | **Partial** | **(2026-07-26, Milestones 11–13)** Estimate/Quotation mode with its own non-fiscal `EST-` series and a rate-choice conversion to a real tax invoice; Sales Return & Credit Note (`CRN-` series, partial returns, pro-rata discount reversal, `Sold → Returned` stock path). Revenue reporting is now document-type aware everywhere. **(2026-07-25, Milestones 2, 7–10)** Core calculation engine (`src/lib/billingCalculations.ts`) is correct and unit-tested against the PRD §17 worked example — per-item wastage %, making-charge-type branching, old-gold settled only at final payment. **Discount now correctly reduces the taxable value *before* GST (PRD §7.4, Milestone 7)** — the previous post-GST order overstated tax on every discounted sale. **PAN/Form 60 gate at ≥₹2L (Milestone 8)**, **multi-tender payment split with exact-sum validation (Milestone 9)**, and **manager-override reason logging persisted to the invoice for audit (Milestone 10)** are all live. Still missing: Advance/Booking module, Repair/Alteration billing, HUID-per-line printing, TCS logic, barcode-scan-to-bill, HSN/CGST-SGST-IGST split (M21). |
| **8. Old Gold & Silver Exchange** | Phase 6 | **Partial** | **(2026-07-26, Milestones 14-15)** Standalone Old Gold module at `/oldgold`: the PRD §8.2 melt/touch valuation engine (`oldGoldValuation.ts`), a purchase voucher with its own `OGV-` series capturing everything §8.4 requires (seller KYC, description, gross weight, tested purity, deduction %, net weight, rate, value, settlement mode, linked invoice), a customer-facing valuation breakdown before confirmation, and an enforced vault lifecycle (`InSafe → SentForMelting → Melted → FineGoldStock`/`ResaleAsIs`) with refining-variance and capital-deployed reporting. Inline trade-in settlement remains correct since M2. **Note:** PRD §17's worked old-gold figures are arithmetically inconsistent with §8.2's formula — see `HANDOFF.md` §1a. Still missing: item photo capture, Margin Scheme (Rule 32(5)) mode, and the damaged-tag→raw-metal melting loop (Milestone 43, distinct from old-gold melting). |
| **9. GST Compliance Module** | Phase 7 | **Partial** | **(2026-07-29, Milestones 21–23)** CGST+SGST vs IGST determined per PRD §7.3 by comparing branch state to customer state, defaulting a stateless walk-in to the shop's own state; halves derived so they always sum to the tax charged. HSN per line (Rule 46) and the correct issuing-branch GSTIN on every invoice. PRD §7.3's **Round Off** line implemented. e-Invoice IRN/QR and e-Way Bill are **simulated** end to end (`src/lib/eInvoice.ts`) with a deterministic 64-hex IRN, a real scannable QR, the 24-hour cancellation window and a retry queue. GSTR-1 (4A/7/9B/12) and GSTR-3B 3.1(a) previews with CSV export and a reconciliation banner (`src/lib/gstReturns.ts`). Missing: no RCM flagging on purchases (M40 depends on the Procurement module), no ITC/GSTR-2B reconciliation (M41), no real GSP/NIC API — simulation is a deliberate ground rule, not a gap; per-state e-Way Bill thresholds land in M34. |
| **10. Accounting & Financial Ledgers** | Phase 8 | **Not Started** | Zero accounting entities or screens exist — no Chart of Accounts, no journal entries, no Ledger Statements, no Trial Balance/P&L/Balance Sheet, no Tally export, no Bank Reconciliation. |
| **11. BIS Hallmarking & HUID** | Phase 9 | **Partial** | **(2026-07-29, Milestone 24)** Real AHC dispatch register (`AHC-<year>-nnn` series) with per-piece receipt outcomes, in a fifth Catalog tab. HUIDs are validated for format and enforced **globally unique across every tag** plus within the batch being received — a HUID can never be reused (PRD §11.1). Certified purity is captured per piece and compared to declared fineness, flagging a shortfall for karigar accountability (§11.3); a failed assay returns the piece to `ReceivedFromKarigar` for rework rather than melting it, via a state-machine edge added for this. **(2026-07-29, Milestone 25)** A non-exempt, un-hallmarked piece can no longer be billed: a configurable guard (block / warn / off) runs as a checkout gate with a live banner, honouring the §11.3 exemptions for metal, category, weight and shop turnover. Manually-typed custom lines are covered too, recording their own HUID, so the guard cannot be bypassed by typing a piece in; a catalogue line's HUID is read-only at the till. Still missing: no certificate file *upload*; no BIS portal reporting; no exemption for antique/export pieces (no flag exists to carry it). |
| **12. Gold Savings Scheme (Swarna Nidhi)** | Phase 10 | **Done** | **(2026-07-29, Milestones 26-27)** Real Scheme Master (configurable tenure, bonus type, fixed-vs-minimum instalment, early-exit penalty) with enrolments, instalment receipts on their own `SR-` series, and a printable passbook. **Balances are derived from the append-only receipts, never stored** - same pattern as M16/M48. The maturity bonus accrues only when matured *and* fully paid, and redemption is gated on the same condition. Missed-instalment tracking and a collection-overdue figure are live, and the total scheme liability is surfaced as the balance-sheet figure PRD 12.4 asks for. The BUIDS Act 2019 cash-refund block is stated on the panel and every passbook, and there is deliberately no cash-out function. Missing: automated SMS/WhatsApp due reminders (a notification-channel concern, M51), and scheme redemption still debits the legacy `Customer.savingsSchemeBalance` at billing rather than a specific enrollment - wiring the two together is a follow-up. |
| **13. CRM, Loyalty & Rate Alerts** | Phase 11 | **Partial** | Loyalty points accrue on customer creation/installments; tier auto-assigns by spend. Missing: Customer 360 view, birthday/anniversary reminders, purchase-history/preference profiling, WhatsApp/SMS rate-alert subscription, campaign broadcast tooling. |
| **14. Reports, Analytics & Dashboards** | Phase 11 | **Partial** | **(2026-07-26, Milestone 13)** Every Dashboard widget is now derived from real state — the hardcoded "Monthly Sales Revenue Trend" SVG and the static "ERP Action Log" are gone, replaced by a unit-tested `dashboardAnalytics.ts` (6-month trend net of returns, excluding estimates; a real activity feed) plus new Stone Vault and Metal-On-Factory-Floor KPI cards. Still missing: no dedicated `/reports` route — the PRD §14.2–14.9 catalog is absent (Milestone 30, task list expanded to all seven report families); no Inventory Dashboard (M44) or Buyback Dashboard (M53). The activity feed reconstructs events from current records rather than logging them as they happen — a real event store arrives with Milestone 50. |
| **15. Security & Statutory Hooks** | Phase 12 | **Partial** | **(2026-07-25, Milestones 8 & 10)** The PAN threshold now exists as a real check (`src/lib/statutoryChecks.ts`, ₹2,00,000, Rule 114B) enforced at checkout, and counter-level price overrides are captured with a mandatory logged reason persisted onto the invoice line — the first real audit trail in the app. Still missing: cosmetic auth only (any credentials work); no Statutory Parameters *config screen* to make the threshold data-driven (M34); TCS/PMLA thresholds still absent; no Supervisor PIN gate on overrides (M33); no general Audit Trail viewer; no maker-checker approval queue. |
| **16. Hardware & Offline Integration** | Phase 13 | **UI Mockup Only** | The "Simulation Desk" fakes API latency and offline errors via `setTimeout` — a deliberate prototype affordance, not real peripheral integration. No Digital Scale connection, no Thermal Printer layout designer, no handheld scanner listener, no real offline-cache-and-sync (this is out of scope for a frontend-only, no-backend prototype — see `.ai/PROJECT_OVERVIEW.md`). |

---

## Since the Last Audit (2026-07-25, Milestones 1, 2 & 3)

Rows **7, 8, and 12** improved from "Partial / Incorrect" to "Partial" — the GST-compliance-breaking calculation bugs (old-gold reducing the taxable base, hardcoded wastage, making-charge-type mishandling, non-functional Scheme Redemption, non-sequential invoice numbers) are fixed and unit-tested. Rows **2 (partially, via `ThemeContext`/lifted state), and the underlying state-architecture issues behind rows 5/6/12** (isolated Stone/JobBag state, duplicated theme detection, stale header search) are also resolved — see `KNOWN_ISSUES.md` #1, #3, #4, #5, #8, #9, #11, #14 and `CHANGELOG.md`'s 2026-07-25 entry for the full list.

**Milestone 3 (2026-07-25):** Rows **4.2** and **5** improved from "UI Mockup Only" to "Partial" (Item Design vs. Tag data model split, resolving Handbook D-6), and row **11** gained a real `huid` field (still "UI Mockup Only" overall, since the AHC assignment *workflow* is still missing).

**Milestones 4–10 (2026-07-25):** Row **15** improved from "Not Started" to "Partial" — the PAN threshold check and the price-override reason log are the first real statutory enforcement and audit trail in the app. Row **5** deepened substantially within "Partial": the Tag lifecycle is now a genuinely enforced 12-state machine rather than a free-text field, with real barcode/QR generation and a Stock Audit screen. Row **7** deepened within "Partial": the discount-before-GST bug is fixed, and the PAN gate, multi-tender split, and override reason log are all live.

No other module status changed in this pass — Modules 1, 2, 3, 4.1/4.3-4.7, 6, 8–14, 16 remain as previously assessed; this pass re-verified rather than assumed that.
